# LittleTiles OC Addon (Minecraft 1.12.2) — Русская версия

📖 [English version](README.md)

Мод-аддон для OpenComputers, который расширяет LittleTiles. Добавляет блок Signal Converter для чтения сигналов кабелей LittleTiles и отправки событий при изменении сигналов.

## Что делает мод

- Добавляет блок **Signal Converter OC**
- Подключается к кабелям LittleTiles (до 4 кабелей, по 4 бита каждый)
- Дает доступ к сигналам через Lua-скрипты OpenComputers
- Отправляет событие **`signal_changed`** при изменении любого сигнала
- Имя компонента: `lt_signal_converter`

## Требования

- Minecraft 1.12.2
- Forge 14.23.x
- Мод LittleTiles
- Мод OpenComputers

## Установка

1. Установите Forge, LittleTiles, OpenComputers
2. Закиньте JAR мода в папку `mods/`
3. Запустите Minecraft

## Использование

### 1. Настройка в игре

1. Поставьте блок **Signal Converter OC**
2. Подключите к нему кабели LittleTiles (с любой стороны)
3. Поставьте рядом компьютер OpenComputers
4. Подключите компьютер к конвертеру кабелем OC или поставьте компьютер вплотную

### 2. Поиск компонента

```lua
local component = require("component")

-- Найти конвертер
local addr = component.get("lt_signal_converter")
if not addr then
  error("Signal Converter не найден! Проверьте подключение.")
end

local converter = component.proxy(addr)
print("Подключено к Signal Converter")
```

### 3. Чтение сигналов (Опрос)

#### Получить общий сигнал (0-15)
Возвращает объединенное значение (OR) со всех кабелей:
```lua
local signal = converter.getSignal()
print("Общий сигнал:", signal) -- 0 до 15
```

#### Проверить конкретный бит
```lua
local isHigh = converter.isSignalHigh(0) -- проверить бит 0
print("Бит 0 включен:", isHigh)
```

#### Получить отдельные биты
```lua
local bit0, bit1, bit2, bit3 = converter.getBits()
print("Биты:", bit0, bit1, bit2, bit3)
```

### 4. Мониторинг через события (Рекомендуется)

Вместо постоянного опроса используйте **`event.pull()`** для ожидания изменений:

```lua
local component = require("component")
local event = require("event")

local addr = component.get("lt_signal_converter")
local converter = component.proxy(addr)

print("Ожидание изменений сигнала...")

while true do
  -- Ждем событие signal_changed (таймаут 10 секунд)
  local _, newSignal = event.pull(10, "signal_changed")
  
  if newSignal then
    print("Сигнал изменился на:", newSignal)
    
    -- Получить детальную информацию
    local bit0 = converter.isSignalHigh(0)
    local bit1 = converter.isSignalHigh(1)
    print("Бит0:", bit0, "Бит1:", bit1)
  else
    print("Изменений не было в течение 10 секунд")
  end
end
```

### 5. Доступ к отдельным кабелям

#### Количество кабелей
```lua
local count = converter.getCableCount()
print("Подключено кабелей:", count)
```

#### Сигнал конкретного кабеля
```lua
local signal = converter.getCableSignal(0)
print("Сигнал кабеля 0:", signal) -- 0 до 15
```

#### Биты конкретного кабеля
```lua
local b0, b1, b2, b3 = converter.getCableBits(0)
print("Биты кабеля 0:", b0, b1, b2, b3)
```

#### Сторона подключения кабеля
```lua
local facing = converter.getCableFacing(0)
print("Кабель 0 на стороне:", facing) -- "north", "south" и т.д.
```

### 6. Информация о всех кабелях

```lua
local cables = converter.getAllCables()

for index, cable in pairs(cables) do
  print("Кабель #" .. index)
  print("  Сторона:", cable.facing)
  print("  Сигнал:", cable.signal)
  print("  Биты:", cable.bit0, cable.bit1, cable.bit2, cable.bit3)
end
```

### 7. Отладочная информация

```lua
local info = converter.debugInfo()
print("Всего кабелей:", info.cableCount)
print("Общий сигнал:", info.combinedSignal)

for i, cable in ipairs(info.cables) do
  print("Кабель " .. i .. ":")
  print("  Сторона:", cable.facing)
  print("  Есть сеть:", cable.hasNetwork)
  print("  Сигнал:", cable.signal)
end
```

## Полные примеры

### Пример 1: Простой монитор сигнала через события

```lua
local component = require("component")
local event = require("event")

local addr = component.get("lt_signal_converter")
if not addr then
  print("Signal Converter не найден!")
  return
end

local sc = component.proxy(addr)
print("Мониторинг сигналов... Нажмите Ctrl+C для остановки")

while true do
  -- Ждем событие изменения сигнала
  local _, signal = event.pull("signal_changed")
  print("Сигнал изменился на:", signal)
  
  -- Проверить отдельные биты
  for i = 0, 3 do
    if sc.isSignalHigh(i) then
      print("  Бит " .. i .. " включен")
    end
  end
end
```

### Пример 2: Мониторинг конкретного кабеля через события

```lua
local component = require("component")
local event = require("event")

local sc = component.proxy(component.get("lt_signal_converter"))

print("Мониторинг кабеля 0...")
local lastSignal = sc.getCableSignal(0)

while true do
  local _, newSignal = event.pull(5, "signal_changed")
  
  if newSignal then
    local current = sc.getCableSignal(0)
    if current ~= lastSignal then
      print("Кабель 0 изменился с", lastSignal, "на", current)
      lastSignal = current
    end
  end
end
```

### Пример 3: Простое управление редстоуном

```lua
local component = require("component")
local event = require("event")
local redstone = component.redstone -- если доступно

local sc = component.proxy(component.get("lt_signal_converter"))

print("Управление редстоуном на основе сигнала...")

while true do
  local _, signal = event.pull("signal_changed")
  
  if signal > 0 then
    print("Сигнал обнаружен! Включаем редстоун.")
    -- Включить выход редстоуна
    if redstone then
      redstone.setOutput(1, 15) -- сторона, уровень мощности
    end
  else
    print("Нет сигнала. Выключаем редстоун.")
    if redstone then
      redstone.setOutput(1, 0)
    end
  end
end
```

## Как это работает

- Конвертер работает как компонент **INPUT** в сетях LittleTiles
- Получает сигналы от подключенных кабелей (до 4 кабелей)
- Каждый кабель передает 4 бита (значения 0-15)
- Когда **любой** сигнал меняется, отправляется событие `signal_changed` с новым объединенным значением
- Несколько кабелей объединяются операцией OR
- Можно использовать как опрос (`getSignal()`), так и события (`event.pull()`)

## Справочник API

| Метод | Аргументы | Возвращает | Описание |
|-------|-----------|------------|----------|
| `getSignal()` | - | `number` | Общий сигнал со всех кабелей (0-15) |
| `isSignalHigh(bit)` | `bit: 0-3` | `boolean` | Проверить включен ли бит |
| `getBits()` | - | `b0, b1, b2, b3` | Все биты (булевы) |
| `getCableCount()` | - | `number` | Количество подключенных кабелей |
| `getCableSignal(index)` | `index: 0+` | `number` | Сигнал конкретного кабеля |
| `getCableBits(index)` | `index: 0+` | `b0, b1, b2, b3` | Биты конкретного кабеля |
| `getCableFacing(index)` | `index: 0+` | `string` | Сторона подключения (north, south и т.д.) |
| `getAllCables()` | - | `table` | Таблица со всеми кабелями |
| `debugInfo()` | - | `table` | Отладочная информация |

### События

| Событие | Аргументы | Описание |
|---------|-----------|----------|
| `signal_changed` | `newSignal: number` | Срабатывает при изменении любого сигнала |

## Решение проблем

**"lt_signal_converter не найден"**
- Убедитесь что блок установлен и компьютер подключен через сеть OC
- Проверьте что к блоку подключены кабели
- Убедитесь что мод загружен в Minecraft

**События не срабатывают**
- Проверьте правильность использования `event.pull()`
- Убедитесь что компьютер подключен к конвертеру
- Попробуйте метод debugInfo() для проверки обнаружения кабелей

**Всегда возвращает 0**
- Проверьте что кабели подключены к блоку конвертера
- Убедитесь что источник сигнала LittleTiles (рычаг, кнопка и т.д.) активен
- Используйте debugInfo() для проверки обнаружения кабелей
